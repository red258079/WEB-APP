server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    # Redirect root to /client/index.html
    location = / {
        return 301 /client/index.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
